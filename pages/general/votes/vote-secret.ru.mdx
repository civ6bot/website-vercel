import TableSyntax from "components/TableSyntax";
import Swiper from "components/Swiper";
import getLocale from "text/getLocale";
import TableConfig from "components/TableConfig";


Создаёт тайное голосование.

## {getLocale("pagesSyntax", "ru")}
<TableSyntax page="vote-secret" lang="ru" />
## {getLocale("pagesDescription", "ru")}

Создаёт в тайное голосование с таймером для ограниченного списка пользователей. Пользователи, участвующие в голосовании, получат личное сообщение от бота, в котором они могут выбрать один из следующих вариантов:
- "за" (галочка);
- "против" (крестик);
- воздержался (эмодзи 🤔); если не выбрать реакцию за отведённое время, то будет считаться, что игрок воздержался от ответа;
    - вариант ответа "воздержался" можно убрать в настройках; в таком случае будет считаться, что игрок ответил "против".

После того, как все пользователи сделают выбор или закончится время, сообщение покажет результат голосования, при этом не показывая выбор каждого из участников.

## {getLocale("pageUsageLimitations", "ru")}

- В голосовании могут принимать участие от 2 до 16 пользователей.
- Предполагается, что при использовании этой команды вы находитесь в публичном голосовом канале. Все присутствующие в нём пользователи участвуют в голосовании по умолчанию. 
    - Если вы не находитесь в голосовом канале, то будет считаться, что в голосовании участвуете только вы (что приведёт к ошибке). В таком случае вам стоит воспользоваться параметрами `include`, `exclude` и `only`.

## {getLocale("pagesConfigs", "ru")}
<TableConfig page="vote-secret" lang="ru" />
## {getLocale("pagesScreenshots", "ru")}
<Swiper page="vote-secret" lang="ru" />
## {getLocale("pagesSeeAlso", "ru")}
- [/vote](./vote)
