import TableSyntax from "components/TableSyntax";
import Swiper from "components/Swiper";
import getLocale from "text/getLocale";


Shows the random outcome of a coin toss (heads or tails).

## {getLocale("pagesSyntax", "en")}
<TableSyntax page="coin" lang="en" />
## {getLocale("pagesDescription", "en")}

A simple command that, when called, shows one of two static message: heads or tails. You can use it as a random number generator from 1 to 2.

## {getLocale("pagesScreenshots", "en")}
<Swiper page="coin" lang="en" />
## {getLocale("pagesSeeAlso", "en")}
- [/random](./random)
