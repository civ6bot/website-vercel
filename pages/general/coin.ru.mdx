import TableSyntax from "components/TableSyntax";
import Swiper from "components/Swiper";
import getLocale from "text/getLocale";


Показывает результат подбрасывания монетки.

## {getLocale("pagesSyntax", "ru")}
<TableSyntax page="coin" lang="ru" />
## {getLocale("pagesDescription", "ru")}

Простая команда, которая при вызове показывает сообщение с результатом подбрасывания монетки: "орёл" или "решка". Вы можете использовать эту команду как генератор случайных чисел от 1 до 2.

## {getLocale("pagesScreenshots", "ru")}
<Swiper page="coin" lang="ru" />
## {getLocale("pagesSeeAlso", "ru")}
- [/random](./random)
